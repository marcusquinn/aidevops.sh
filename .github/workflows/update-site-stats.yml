name: Update site stats

on:
  workflow_dispatch:
  schedule:
    - cron: '17 2 * * *'

concurrency:
  group: update-site-stats-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-site-stats:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate site stats
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 scripts/update_site_stats.py

      - name: Commit updated stats
        run: |
          if git diff --quiet -- data/aidevops-stats.json; then
            echo "No site stats changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/aidevops-stats.json
          git commit -m "chore: update site stats"
          git push
